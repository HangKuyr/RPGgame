#ifndef ENUMTYPE_H
#define ENUMTYPE_H

/** @file enum_type.hpp
 *	 File contain enum class type
 */

/** @enum Type
 *  @brief Indicates the type of a room
 *  @author Lais Lima
 */
enum class Type{
	BATTLE,      /**< Coded battle as the type. */
	INTERACTION  /**< Coded interaction as the type. */
};

#endif